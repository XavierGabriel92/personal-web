interface TrainerClientSettingsPageProps {
  clientId: string;
}

export default function TrainerClientSettingsPage({ clientId }: TrainerClientSettingsPageProps) {
  return <div>Settings {clientId}</div>
}

