/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/client.ts";
import type { GetApiRoutinesQueryResponse } from "../types/GetApiRoutines.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/client.ts";

function getGetApiRoutinesUrl() {
  const res = { method: 'GET', url: `/api/routines` as const }  
  return res
}

/**
 * @description Get all routines for the authenticated user
 * @summary List all routines
 * {@link /api/routines}
 */
export async function getApiRoutines(config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const res = await request<GetApiRoutinesQueryResponse, ResponseErrorConfig<Error>, unknown>({ method : "GET", url : getGetApiRoutinesUrl().url.toString(), ... requestConfig })  
  return res.data
}