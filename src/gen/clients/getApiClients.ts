/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import fetch from "@/lib/client.ts";
import type { GetApiClientsQueryResponse } from "../types/GetApiClients.ts";
import type { RequestConfig, ResponseErrorConfig } from "@/lib/client.ts";

function getGetApiClientsUrl() {
  const res = { method: 'GET', url: `/api/clients` as const }  
  return res
}

/**
 * @description Get all clients for the authenticated user
 * @summary List all clients
 * {@link /api/clients}
 */
export async function getApiClients(config: Partial<RequestConfig> & { client?: typeof fetch } = {}) {
  const { client: request = fetch, ...requestConfig } = config  
  
  const res = await request<GetApiClientsQueryResponse, ResponseErrorConfig<Error>, unknown>({ method : "GET", url : getGetApiClientsUrl().url.toString(), ... requestConfig })  
  return res.data
}